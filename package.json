{
  "name": "ai-usage",
  "version": "0.1.8",
  "description": "A cross-platform desktop dashboard for local AI coding usage statistics.",
  "desktopName": "ai-usage",
  "license": "MIT",
  "author": {
    "name": "peipeitu",
    "email": "peipeitu@users.noreply.github.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/peipeitu/ai-usage.git"
  },
  "bugs": {
    "url": "https://github.com/peipeitu/ai-usage/issues"
  },
  "homepage": "https://github.com/peipeitu/ai-usage#readme",
  "type": "commonjs",
  "scripts": {
    "dev": "tauri dev",
    "start": "tauri dev",
    "icons": "node scripts/generate-icons.js",
    "updater:latest": "node scripts/write-latest-json.js",
    "updater:verify-signatures": "node scripts/verify-updater-signatures.js",
    "release:check": "node scripts/check-release.js",
    "release:verify": "node scripts/verify-updater-release.js",
    "test": "node --check src/renderer/renderer.js && node --check src/renderer/tray.js && node --check scripts/check-release.js && node --check scripts/package-macos-local.js && node --check scripts/release-policy.js && node --check scripts/write-updater-config.js && node --check scripts/write-latest-json.js && node --check scripts/verify-updater-release.js && node --check scripts/verify-updater-signatures.js && node --test tests/renderer.test.js tests/tray.test.js tests/release-check.test.js tests/release-policy.test.js tests/updater-manifest.test.js tests/updater-release.test.js tests/updater-signature.test.js && cargo test --manifest-path src-tauri/Cargo.toml",
    "fmt:check": "cargo fmt --manifest-path src-tauri/Cargo.toml -- --check",
    "lint": "cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings",
    "package:mac": "node scripts/package-macos-local.js",
    "package:win": "node scripts/write-updater-config.js && tauri build --config src-tauri/tauri.updater.generated.conf.json --bundles nsis",
    "package:win:unsigned": "tauri build --bundles nsis",
    "package:linux": "node scripts/write-updater-config.js && tauri build --config src-tauri/tauri.updater.generated.conf.json --bundles appimage,deb",
    "package:linux:unsigned": "tauri build --bundles appimage,deb"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
